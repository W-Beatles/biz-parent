<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <!--mysql 连接数据库jar 这里选择自己本地位置-->
    <classPathEntry
            location="D:\mysql-connector-java-8.0.13.jar"/>

    <context id="default" targetRuntime="MyBatis3">
        <!-- 指定生成的java文件的编码 -->
        <property name="javaFileEncoding" value="UTF-8"/>

        <!-- 不生成注释和时间戳 -->
        <commentGenerator>
            <property name="suppressDate" value="true"/>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <!-- 数据库连接的信息 -->
        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://localhost:3306/xunwu"
                        userId="root"
                        password="123456">
        </jdbcConnection>

        <!-- 类型处理器：默认false，把 DECIMAL 和 NUMERIC 类型解析为Integer，为true时解析为java.math.BigDecimal -->
        <javaTypeResolver>
            <property name="forceBigDecimals" value="false"/>
        </javaTypeResolver>

        <!--  Model生成器：生成entity及Example类 -->
        <javaModelGenerator targetPackage="cn.waynechu.dal.entity"
                            targetProject="src/main/java">
            <!-- enableSubPackages：是否添加schema作为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
            <!-- 是否对model添加 构造函数 -->
            <property name="constructorBased" value="true"/>
            <!-- 是否对类CHAR类型的列的数据进行trim操作 -->
            <property name="trimStrings" value="true"/>
            <!-- 建立的Model对象是否 不可改变  即生成的Model对象不会有 setter 方法，只有构造方法 -->
            <property name="immutable" value="false"/>
        </javaModelGenerator>

        <!-- mapper.xml映射文件生成的位置 -->
        <sqlMapGenerator targetPackage="sqlmap"
                         targetProject="src/main/resources">
            <property name="enableSubPackages" value="false"/>
        </sqlMapGenerator>

        <!-- mapper接口生成的位置 -->
        <javaClientGenerator type="XMLMAPPER"
                             targetPackage="cn.waynechu.dal.mapper"
                             targetProject="src/main/java">
            <property name="enableSubPackages" value="false"/>
            <property name="useGeneratedKeys" value="true"/>
        </javaClientGenerator>

        <table tableName="house"/>
    </context>
</generatorConfiguration>